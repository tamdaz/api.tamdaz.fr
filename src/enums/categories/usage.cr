# Consists of identifying the category usage. For example, if a category is used for blogs,
# it will contain a collection of blogs. This applies also for reports and projects.
enum App::Enums::Categories::Usage
  Blogs
  Projects
  Reports
end
